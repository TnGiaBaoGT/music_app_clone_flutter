class UserEntity {
  // final String ? userId;
   String ? fullName;
   String ? email;
   String ? imageUrl;

  UserEntity({
     // this.userId,
     this.email,
     this.fullName,
     this.imageUrl,
});
}